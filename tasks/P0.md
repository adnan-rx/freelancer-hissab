# FreelancerHisab — P0 Tasks (Rescue & Hackathon Submission)

Target Deadline: Monday Submission  
Focus: Core End-to-End Workflow (Upwork CSV Import -> PKR Tax Engine -> Live Exchange Rates -> Professional PDF Invoices)

---

### TASK-001: Fix Frontend TypeScript Build Error & Tabs Component
- **Files:** `apps/web/src/components/ui/tabs.tsx`, `apps/web/src/app/(dashboard)/settings/page.tsx`
- **Objective:** Replace dummy `tabs.tsx` wrapper with a proper interactive state/context implementation supporting Radix or React state so `value` props on `TabsContent` compile and tab navigation works.
- **Acceptance Criteria:**
  - `npm run check-types` passes cleanly across the entire workspace.
  - Tab switching between Profile, Bank & Tax, Invoicing, and Security on Settings page works without errors.

---

### TASK-002: Real Upwork / Fiverr CSV Parser Endpoint
- **Files:** `apps/api/src/modules/csv/` (New Module), `apps/api/src/app.module.ts`, `apps/web/src/components/features/csv-import-modal.tsx`
- **Objective:** Create a real NestJS endpoint `POST /api/v1/csv/import` using `multer` or stream parsing that ingests Upwork Transaction History CSVs and Fiverr CSV statements, parses income, platform fee expenses, client names, transaction dates, and inserts them into PostgreSQL.
- **Acceptance Criteria:**
  - Dragging a real Upwork CSV into the modal sends `multipart/form-data` to API.
  - API extracts transactions, creates missing clients automatically, logs income in USD/PKR, and records fees as expenses.
  - Modal displays actual count of imported records.

---

### TASK-003: Pakistan FBR Tax Calculation Engine
- **Files:** `apps/api/src/modules/tax/` (New Module), `apps/api/src/database/schema/tax-profiles.ts`, `apps/web/src/app/(dashboard)/reports/page.tsx`
- **Objective:** Implement Pakistan Income Tax Ordinance 2001 (Section 154A) tax calculation service.
  - 0.25% reduced tax rate for PSEB registered IT/ITeS exporters.
  - 1.0% tax rate for non-PSEB IT exporters.
  - Standard slab rates for local income.
  - Exemption threshold tracking.
- **Acceptance Criteria:**
  - API endpoint `GET /api/v1/tax/estimate` returns detailed tax breakdown (Gross Export Revenue, PSEB Exemption Savings, Tax Liability in PKR, FBR Filing Status).
  - Reports page visualizes the real calculated tax liability based on database income records.

---

### TASK-004: Live / Cached Exchange Rate Service
- **Files:** `apps/api/src/modules/exchange-rate/` (New Module), `apps/api/src/modules/income/income.service.ts`, `apps/api/src/modules/invoices/invoices.service.ts`
- **Objective:** Replace static fallback `280` exchange rate with a live exchange rate service (fetching USD/EUR/GBP to PKR rates from SBP / OpenExchangeRates / ExchangeRate-API with 24h caching).
- **Acceptance Criteria:**
  - Income and Invoices dynamically fetch today's actual PKR conversion rate when `exchangeRate` is omitted.
  - Fallback to latest cached rate if API is unavailable.

---

### TASK-005: SBP Proceeds Realization Certificate (PRC) & Remittance Tracker
- **Files:** `apps/api/src/database/schema/remittances.ts`, `apps/api/src/modules/remittances/`, `apps/web/src/app/(dashboard)/income/page.tsx`
- **Objective:** Add SBP purpose code fields (`9100` - Software Export, `9101` - IT Services) and PRC tracking number logging to foreign income transactions.
- **Acceptance Criteria:**
  - Income creation supports `prcReferenceNumber` and `sbpPurposeCode`.
  - Income page displays PRC verification badge for compliant remittances.

---

### TASK-006: Server-side API Input Validation & Security Guardrails
- **Files:** `apps/api/src/main.ts`, `apps/api/src/app.module.ts`
- **Objective:** Enable global `ValidationPipe({ whitelist: true, transform: true })` and `ThrottlerModule` in NestJS backend.
- **Acceptance Criteria:**
  - Invalid request bodies return 400 Bad Request with validation details.
  - Throttler guard prevents brute-force attempts on `/auth/login`.

---

### TASK-007: Domain Unit & E2E Testing Suite
- **Files:** `apps/api/src/modules/tax/tax.service.spec.ts`, `apps/api/src/modules/csv/csv.service.spec.ts`, `apps/api/src/modules/invoices/invoices.service.spec.ts`
- **Objective:** Add comprehensive Jest unit tests covering:
  - FBR Section 154A Tax calculation formulas (PSEB vs Non-PSEB).
  - CSV parsing edge cases (empty rows, negative values, unknown column names).
  - Invoice total & PKR calculation formulas.
- **Acceptance Criteria:**
  - `npm run test` passes with >85% coverage on tax and CSV parsing logic.
