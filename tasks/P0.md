# FreelancerHisab — P0 Tasks (Rescue & Hackathon Submission)

Target Deadline: Monday Submission  
Focus: Core End-to-End Workflow (Upwork CSV Import -> PKR Tax Engine -> Live Exchange Rates -> Professional PDF Invoices)

---

### TASK-001: Fix Frontend TypeScript Build Error & Tabs Component — ✅ COMPLETED
- **Files:** `apps/web/src/components/ui/tabs.tsx`, `apps/web/src/app/(dashboard)/settings/page.tsx`
- **Status:** ✅ COMPLETED & VERIFIED
- **Summary:** Re-architected `tabs.tsx` with React Context to support value props and active state. `npm run check-types` passes cleanly across the entire workspace.

---

### TASK-002: Real Upwork / Fiverr CSV Parser Endpoint — ✅ COMPLETED
- **Files:** `apps/api/src/modules/csv/`, `apps/api/src/app.module.ts`, `apps/web/src/components/features/csv-import-modal.tsx`
- **Status:** ✅ COMPLETED & VERIFIED
- **Summary:** Implemented `POST /api/v1/csv/import` in NestJS with Upwork/Fiverr/Generic CSV transaction parsing, auto client creation, income log creation, and fee expense creation. Wired `csv-import-modal.tsx` to upload CSVs via multipart Axios request. Added `csv.service.spec.ts` unit tests.

---

### TASK-003: Pakistan FBR Tax Calculation Engine — ✅ COMPLETED
- **Files:** `apps/api/src/modules/tax/`, `apps/api/src/app.module.ts`, `apps/web/src/hooks/use-reports.ts`, `apps/web/src/app/(dashboard)/reports/page.tsx`
- **Status:** ✅ COMPLETED & VERIFIED
- **Summary:** Implemented Pakistan Income Tax Ordinance 2001 Section 154A logic (0.25% PSEB vs 1.0% non-PSEB export tax, local progressive tax slabs, PSEB savings calculation). Exposed `GET /api/v1/tax/estimate` endpoint. Connected reports page to render dynamic tax liability. Added unit tests `tax.service.spec.ts`.

---

### TASK-004: Live / Cached Exchange Rate Service — ✅ COMPLETED
- **Files:** `apps/api/src/modules/exchange-rate/`, `apps/api/src/app.module.ts`
- **Status:** ✅ COMPLETED & VERIFIED
- **Summary:** Implemented `ExchangeRateService` fetching live USD, EUR, GBP to PKR conversion rates from ExchangeRate-API with 24-hour in-memory cache fallback. Exposed `GET /api/v1/exchange-rate` & `GET /api/v1/exchange-rate/convert`. Added unit tests `exchange-rate.service.spec.ts`.

---

### TASK-005: SBP Proceeds Realization Certificate (PRC) & Remittance Tracker — ✅ COMPLETED
- **Files:** `apps/api/src/database/schema/income.ts`, `apps/api/src/modules/income/income.service.ts`, `apps/api/src/modules/income/dto/income.dto.ts`
- **Status:** ✅ COMPLETED & VERIFIED
- **Summary:** Added `sbpPurposeCode` (default `'9100'` - Software Export) and `prcReferenceNumber` columns to Drizzle `income` schema, DTOs, and service persistence layer.

---

### TASK-006: Server-side API Input Validation & Security Guardrails — ✅ COMPLETED
- **Files:** `apps/api/src/main.ts`, `apps/api/src/app.module.ts`
- **Status:** ✅ COMPLETED & VERIFIED
- **Summary:** Configured `ThrottlerModule` rate-limiting (100 req/min) with global `ThrottlerGuard` in `AppModule`. Configured strict `ValidationPipe` options (`whitelist: true`, `forbidNonWhitelisted: true`, `transform: true`) in `main.ts`.

---

### TASK-007: Domain Unit & E2E Testing Suite
- **Files:** `apps/api/src/modules/tax/tax.service.spec.ts`, `apps/api/src/modules/csv/csv.service.spec.ts`, `apps/api/src/modules/invoices/invoices.service.spec.ts`
- **Objective:** Add comprehensive Jest unit tests covering:
  - FBR Section 154A Tax calculation formulas (PSEB vs Non-PSEB).
  - CSV parsing edge cases (empty rows, negative values, unknown column names).
  - Invoice total & PKR calculation formulas.
- **Acceptance Criteria:**
  - `npm run test` passes with >85% coverage on tax and CSV parsing logic.
